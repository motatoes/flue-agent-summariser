{
  "name": "flue-agent-summariser-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dry-run": "flue run hn-summary --target node --env .env --input",
    "dry-run:raw": "node --env-file=.env src/local.js",
    "check": "node --check src/local.js && node --check src/hn.js",
    "flue:build": "flue build --target cloudflare",
    "flue:dev": "flue dev --target cloudflare",
    "deploy": "wrangler deploy --config dist/flue_agent_summariser_v1/wrangler.json"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@flue/runtime": "latest",
    "@flue/slack": "^1.0.0-beta.1",
    "@slack/web-api": "^8.0.0",
    "agents": "^0.14.2",
    "hono": "latest",
    "valibot": "latest"
  },
  "devDependencies": {
    "@flue/cli": "latest",
    "wrangler": "latest"
  }
}
